"use strict";

/**
 * 
 * @param {*} a 
 * @param {*} b 
 */
 function addSum(a,b){
     return a + b;
 }
  let result = addSum(4,5);
  console.log(result);

  /**
   * 
   * @param {*} a 
   * @param {*} b 
   */
  function subSum(a,b){
      return a - b;
  }
  let result =subSum(6,2);
  alert(result);

  
   /**
    * 
    * @param {*} a 
    * @param {*} b 
    */

  function multiplication(a,b){
      return a * b; 
  }
  let result = multiplication(8,8);
  alert(result);

function divSum(a,b){
    return a / b;
}
let result=divSum(5,5);
alert(result);